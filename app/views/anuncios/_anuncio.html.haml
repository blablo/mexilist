%li.span2.anuncio
  .thumbnail
    =link_to(image_tag(anuncio.main_image), city_anuncio_url(anuncio))
    %h3=link_to(truncate(anuncio.title, :length => 40, :separator => ' '), city_anuncio_url(anuncio))
    %span.price=number_to_currency(anuncio.price, :precision => 0)
