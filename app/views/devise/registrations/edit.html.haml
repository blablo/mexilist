%h2 Editar cuenta
=simple_form_for(resource, :as => resource_name, :url => registration_path(resource_name), :html => { :method => :put, :class => 'form-vertical' }) do |f|
  =f.error_notification
  =display_base_errors resource
  =f.input :name, :autofocus => true, :label => "Nombre"
  =f.input :email, :required => true, :input_html => {:disabled => :disabled}
  -#f.input :password, :autocomplete => "off", :hint => "ponlo en blanco si no quieres cambiarla", :required => false, :label => "Contraseña"
  -#f.input :password_confirmation, :required => false, :label => "Confirmar contraseña"
  -#f.input :current_password, :hint => "necesitamos tu contraseña actual para confirmar los cambios", :required => true, :label => "Contraseña actual"
  =f.button :submit, 'Actualizar', :class => 'btn-primary'

-# %h3 Cancel my account
-# %p Unhappy? #{link_to "Cancel my account", registration_path(resource_name), :data => { :confirm => "Are you sure?" }, :method => :delete}.
-# =link_to "Back", :back
